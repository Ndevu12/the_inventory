"""Django admin configuration for the sales app.

Customer is managed via Wagtail snippets. Tenant-scoped models
(SalesOrder, Dispatch) are managed via the tenant frontend
dashboard, not platform admin.
"""
