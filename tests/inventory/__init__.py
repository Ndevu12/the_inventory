"""Inventory app test suite.

Tests are organized into subpackages mirroring the source layout:

- ``test_models/`` — Unit tests for all inventory models
- ``test_services/`` — Integration tests for service-layer logic
- ``test_filters.py`` — FilterSet and custom filter tests
- ``test_views.py`` — Admin view tests
- ``test_panels/`` — Dashboard panel component tests
"""
